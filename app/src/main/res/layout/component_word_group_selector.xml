<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvGroupSelectorTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="选择词库"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Spinner
        android:id="@+id/spinnerWordGroup"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintBottom_toBottomOf="@id/tvGroupSelectorTitle"
        app:layout_constraintEnd_toStartOf="@id/btnVoiceInput"
        app:layout_constraintStart_toEndOf="@id/tvGroupSelectorTitle"
        app:layout_constraintTop_toTopOf="@id/tvGroupSelectorTitle" />

    <ImageButton
        android:id="@+id/btnVoiceInput"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_mic_button"
        android:contentDescription="语音输入"
        android:src="@drawable/ic_microphone"
        android:tint="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@id/tvGroupSelectorTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tvGroupSelectorTitle" />

</androidx.constraintlayout.widget.ConstraintLayout> 